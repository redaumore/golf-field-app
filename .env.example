# Google Sheets API URL for the Google Apps Script Web App
# Set a different URL per environment (dev, preview, production).
# Defaults to the production spreadsheet if not set.
# VITE_GOOGLE_SHEETS_API_URL=https://script.google.com/macros/s/YOUR_SCRIPT_ID/exec
